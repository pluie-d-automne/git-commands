# git-commands
git remote add origin git@github.com:pluie-d-automne/first-project.git

git remote -v

git push -u origin main

git log --oneline


```mermaid
  graph TD;
      A-->B;
      A-->C;
      B-->D;
      C-->D;
```